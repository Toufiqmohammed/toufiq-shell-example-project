name: Test EC2 Runner

on: [push]

jobs:
  test-job:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: Say Hello
        run: |
          echo "Success! The workflow is executing directly inside my Ubuntu EC2 server!"
          uname -a
